{
  "name": "bead-storefront-modular",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:data": "node scripts/build-data.mjs",
    "build": "npm run build:data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}